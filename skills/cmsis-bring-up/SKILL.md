---
name: cmsis-bring-up
description: "Establish documentation-led, verifiable device debug and trace facts for an SoC or board before CMSIS-Pack debug content is authored: the debug access topology (processor association, debug-port and access-port path, protocol, dormant state), reset, unlock, authentication, boot, flash and low-power debug behaviour, CoreSight trace paths and components, board connector, jumper and pin-routing choices, supplementary pyOCD topology scans, pyOCD availability, and recovery of moved or broken vendor documentation URLs. Use when the user asks how a device is debugged, what its debug or trace topology is, wants a debug or trace knowledge record (.agent-artifacts) built or refreshed, or before generating PDSC debug or trace sequences; this skill routes to the specialised bring-up skills installed next to it. No live target connection is required; for a live session use cmsis-debug-live."
---

# Device debug and trace knowledge (entry point)

This skill does no work of its own. It selects one of the member skills below and hands
over to it. Every `$name` is a skill installed next to this one — `../<name>/SKILL.md`
relative to this file — and the member skills are the source of truth for their own
prerequisites, steps and guardrails.

## Pick the skill

| If the user wants to… | Hand over to |
|---|---|
| Document board-level debug and trace knowledge. | `$board-debug-knowledge` |
| Find pyOCD or the CMSIS Debugger bundle. | `$check-pyocd-availability` |
| Verify reusable CMSIS debug access facts. | `$debug-access-knowledge` |
| Document reset and low-power debug behavior. | `$debug-knowledge` |
| Capture supplementary CMSIS debug scan evidence. | `$pyocd-detect-debug-topology` |
| Recover authoritative vendor device documentation. | `$resolve-official-device-documentation` |
| Document SoC CoreSight trace topology | `$trace-knowledge` |

## Typical workflow

1. `$debug-access-knowledge` first — the access topology every other record builds on.
2. `$debug-knowledge` and/or `$trace-knowledge` for behaviour and trace facts; `$board-debug-knowledge` only for an explicitly board-specific question.
3. `$check-pyocd-availability`, then `$pyocd-detect-debug-topology`, when documentation leaves a required observation open.
4. `$resolve-official-device-documentation` whenever a vendor document link is dead or has moved.

## How to hand over

- Invoke the chosen skill through your harness's skill mechanism (a Skill tool, `$name`,
  `/name`). If it is not offered, read `../<name>/SKILL.md` and follow it verbatim.
- Honour its *Prerequisites* — if it names another `$skill`, run that one first — and apply
  its *Guardrails*. Do not merge, reorder or paraphrase steps across member skills.
- If `../<name>/SKILL.md` is missing, say so instead of improvising: the skill was not
  installed. The user can add it with **CMSIS Developer Assistant: Select Agent Skills** in VS Code or by editing the
  `cmsis-developer-assistant.installedSkills` setting.

## Member skills

- `$board-debug-knowledge` — Document a named board's supported debug and trace connector, jumper, pin-routing, and configuration choices without narrowing device-level Pack support.
- `$check-pyocd-availability` — Prefer the active Visual Studio Code CMSIS Debugger extension's bundled pyOCD, or otherwise check whether pyOCD is runnable from the active shell.
- `$debug-access-knowledge` — Build a documentation-led, verifiable record of an existing SoC's reusable CMSIS debug access topology: processor association, debug port and access-port path, protocol, and dormant-state decision.
- `$debug-knowledge` — Build a documentation-led, verifiable knowledge record of an existing SoC's reset, unlock, authentication, boot, flash, processor-control, and low-power debug behavior after `$debug-access-knowledge` has established the reusable access topology.
- `$pyocd-detect-debug-topology` — Capture supplementary, non-authoritative CMSIS debug and CoreSight topology evidence from the complete stdout/stderr of `pyocd gdbserver -vv --target cortex_m` after `$check-pyocd-availability` succeeds.
- `$resolve-official-device-documentation` — Recover or conclusively account for an unavailable vendor device-documentation URL by finding the current official source and returning a traceable retrieval result.
- `$trace-knowledge` — Build a documentation-led, verifiable knowledge record of an existing SoC's CoreSight trace paths, components, operating requirements, and constraints for subsequent generation of PDSC trace sequences.

_Generated by `npm run skills:sync` from the category "bring-up" of
Open-CMSIS-Pack/cmsis-agent; edit `scripts/skills.config.json`, not this file._
